package util

case class Read() {

  //
  //  def prettyPrint(b: Board): String = {
  //    val x = b.xy(0)
  //    val y = b.xy(1)
  //    var cellPos = 0
  //
  //    val checkPos = (i: Int, j: Int, c: Array[Cell]) => i == c(cellPos).xy(1) && j == c(cellPos).xy(0)
  //    val s = String
  //
  //    for (i <- 0 until y;
  //         j <- 0 until x) {
  //      if (b.cells.last.absPos + 1 != cellPos && checkPos(i, j, b.cells)) {
  //        print(Console.BLUE + "i" + Console.WHITE)
  //        cellPos += 1
  //      } else
  //        s + " "
  //      s + "\n"
  //    }
  //    s
  //  }
}
